<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Todos</title>
    <link href="https://fonts.googleapis.com/css?family=Julius+Sans+One" rel="stylesheet">
    <!-- UIkit CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/uikit/3.0.0-beta.31/css/uikit.min.css" />
    <link rel="stylesheet" href="/static/css/style.css">
    <!-- UIkit JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/uikit/3.0.0-beta.31/js/uikit.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/uikit/3.0.0-beta.31/js/uikit-icons.min.js"></script>

    <script src="https://use.fontawesome.com/159244af41.js"></script>
</head>
<body>
    <div class="wrapper">
        <h1 class="title">todos</h1>
        <form class="todoForm" action="/todos/add" method="post">
            <input class="uk-input todoInput" type="text" name="description" id="todo" placeholder="Add todo">
            <button type="submit" class="uk-button uk-button-primary uk-width-1-1 todoButton">Submit</button>
        </form>
        <ul class="todos uk-list uk-list-striped">
            {{#each todos}}
                {{#if completed}}
                    <li class="description">
                        <i class="fa fa-check-circle fa-3x mark complete" aria-hidden="true"></i>
                        <div class="todoItem">
                            {{this.description}}
                        </div>
                        <form id="deleteForm" action="/todos/delete/{{this.id}}" method="post">
                            <button class="deleteButton" type="submit">
                                <i class="fa fa-times" aria-hidden="true"></i>
                            </button>
                        </form>
                    </li>
                {{else}}
                    <li class="description">
                        <form class="incompleteForm" action="/todos/done/{{this.id}}" method="post">
                            <button class="incompleteButton  mark" type="submit"><i class="fa fa-circle-thin fa-4x" aria-hidden="true"></i></button>
                        </form>
                        <div class="todoItem">
                            {{this.description}}
                        </div>
                        <form id="deleteForm" action="/todos/delete/{{this.id}}" method="post">
                            <button class="deleteButton" type="submit"><i class="fa fa-times" aria-hidden="true"></i></button>
                        </form>
                    </li>
                {{/if}}
            {{/each}}
        </ul>
    </div>

<script src="/static/js/jquery-3.2.1.min.js"></script>
</body>
</html>